class Arithmetic {
    static summation(x, y) {
        return x + y;
    }

    static subtraction(x, y) {
        return x - y;
    }

    static multiplication(x, y) {
        return x * y;
    }

    static division(x, y) {
        return x / y;
    }
}

export default Arithmetic;